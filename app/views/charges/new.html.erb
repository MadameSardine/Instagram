<%= form_tag post_charges_path(@post) do %>
  <article>
    <label class="amount">
      <span>Amount: £10.00</span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.application.secrets.stripe_test_publishable_key %>"
          data-currency="GBP"
          data-description="Buy picture"
          data-amount="1000"></script>
<% end %>